package mx.edu.utez.sisa.academic_config.domain.model;

/**
 * Classification of a {@link Subject} within its {@link PlanLevel} (spec:
 * "Add and Update Subject").
 */
public enum SubjectType {
	CORE,
	ELECTIVE,
	INTERNSHIP
}
