package mx.edu.utez.sisa.academic_config.domain.model;

/**
 * Distinguishes whether a {@link PlanLevel} carries regular classes or a
 * professional internship period (spec: "Add and Update Plan Level").
 */
public enum PlanLevelType {
	REGULAR,
	INTERNSHIP
}
